# Extract operator

See
[`magrittr::extract`](https://magrittr.tidyverse.org/reference/aliases.html)
for details. This works well in conjunction with the concurrence_matrix
function to extract specific rows and columns from the resulting matrix.

## Usage

``` r
x |> extract(i, j)
```
